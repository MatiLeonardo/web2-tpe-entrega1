
<div>
    <table>
        <thead>
            <tr>
                <td>Número</td>
                <td>Nombre</td>
                <td>Artista</td>
                <td>Album</td>
            </tr>    
        </thead>
        <tbody>
        <?php foreach ($canciones as $cancion): ?>
            <tr> 
                <td><?=$cancion->id_cancion?></td>   
                <td><a href="cancion/<?=$cancion->id_cancion?>"><?=$cancion->nombre_cancion?></a></td>
                <td><?=$cancion->duracion?></td>
                <td><?=$cancion->nombre_artista?></td>
                <?php if (SesionHelper::isAdmin()): ?>
                    <td><a href='borrar/<?= $cancion->id_cancion ?>'>Eliminar</a></td>
                <?php endif; ?>
            </tr>
        <?php endforeach; ?>
        </tbody>    
    </table>
</div>
